# Chanllenges 

## 1. Implement a program that reads data from a CSV file into a vector and calculates simple summary statistics lke min, max, and mean on one colunn.

## 2. Build a hash map that counts the frequency of words in a large text file. Handle capialization and punctuation to combine different forms of the same word 

## 3. Create a linked-list of the common words in a text file. Then, sort the list alphabetically and print the top 10 most common words.

## 4. Develop a function determining if a graph defined using Rust's graph data structures is fully connected.

## 5. Implement Kosaraju's algorithm for detecting strongly connected components in a directed graph. 